@using Model
@model ResponceAndDataList<DepositDto>
@{
    ViewBag.Title = "Месторождения";
}

<h3>Список месторождений</h3>

@if (Model.Responce.Succeded)
{
    if (Model.Data.Any())
    {
        var number = 1;
        <div>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Название</th>
                        <th scope="col" class="col-md-3">Скважины месторождения</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var deposit in Model.Data)
                    {
                        <tr id="@deposit.ID">
                            <td>@number</td>
                            <td>@deposit.Name</td>
                            <td class="centered">
                                <a href="@Url.Action("Wells","Data", new { depositId = deposit.ID, depositName = deposit.Name })"><i class="glyphicon glyphicon-circle-arrow-right"></i></a>
                            </td>
                        </tr>
                        number++;
                    }
                </tbody>
            </table>
        </div>

        <br />
    }
    else
    {
        <div style="margin: 10px">
            <span>Месторождения не найдены</span>
        </div>
    }
}
else
{
    <span>При обработке запроса произошла ошибка.</span>
    <span>@Model.Responce.ErrorMessage</span>
}
